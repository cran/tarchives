# tar_archive_pipelines() errors for a missing package

    Code
      tar_archive_pipelines("notapackage")
    Condition
      Error in `tar_archive_pipelines()`:
      ! Can't find package notapackage.
      i Is it installed?

